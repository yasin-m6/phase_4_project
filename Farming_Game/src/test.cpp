#include <iostream>
#include <vector>
#include<ctime>
#include <string>
#include <algorithm>
#include<chrono>
#include<thread>

using namespace std;



int main()
{
    /*vector<vector<string>> a;
    a.push_back(vector<string>());
    a[0].push_back("ali");
    a[0].push_back("reza");
    
    cout << a.size() << "\t" << a[0].size() << endl;
    a.push_back(vector<string>());
    a[1].push_back("mamad");
    a[1].push_back("amir");
    cout << a.size() << "\t" << a[1].size() << endl;
    a.push_back(vector<string>());
    a[2].push_back("pouya");
    a[2].push_back("yasin");
    cout << a.size() << "\t" << a[2].size() << endl;

    a[1].erase(a[1].begin() + 1);
    cout << a.size() << "\t" << a[1].size() << endl;

    cout << a[0][1];

    this_thread::sleep_for(chrono::seconds(1));*/

    vector<int> a;
    a.push_back(1);
    a.push_back(1);
    a.push_back(1);
    cout << a.size() << endl;
    

}